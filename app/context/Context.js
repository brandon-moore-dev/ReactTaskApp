import { createContext } from 'react';

// Context variable for current selected page
export const CurrentPageContext = createContext(null);

// Context variable for count of pages in Task list
export const PageCountContext = createContext(null);
